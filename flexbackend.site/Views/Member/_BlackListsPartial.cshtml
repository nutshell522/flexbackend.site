@model IEnumerable<EFModels.EFModels.BlackList>

<table>
    <tr>
        <th>行為</th>
    </tr>
    @foreach (var item in Model)
    {
        <tr>
            <td>@item</td>
        </tr>
    }
</table>
